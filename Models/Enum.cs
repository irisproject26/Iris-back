namespace IRIS_API.Models
{
    // Enum para as denúncias (Imagem 2)
    public enum UrgencyLevel
    {
        Baixa,
        Media,
        Alta
    }

    // Enum para o fluxo de aprovação de visitas (Imagem 3)
    public enum VisitaStatus
    {
        Pendente,
        Aceito,
        Recusado
    }
}